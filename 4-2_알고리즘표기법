it has Big-o and omega algorithm
알고리즘의 표기법에는 Big-O표기법과 Omega표기법이 있다.
Big-O의 표기법
여기서의 O는 in the order of 를 뜻하는 것으로 쉽게 생각하면 ~의 정도로 커지는 이라고 생각할 수 있다.
실행시간의 상한을 알아보는 표기법이다. 최악의 경우를 생각하는 것이다.
그래서 big-o의 상한이 낮은 걸 더 좋은 함수라고 본다.
big-o의 종류는 5가지가 있다.

O(n^2)
O(nlogn)
O(n) linear search
O(logn) binary search
O(1)

Omega 표기법
omega표기법은 실행시간의 하한을 알아보는 함수로 최선의 값을 알아보는 것이다.
여기서도 종류가 5가지 있다.

Ω(n^2)
Ω(n log n)
Ω(n) - 배열 안에 존재하는 값의 개수 세기 _ 숫자세기 같은 경우가 있다
Ω(log n)
Ω(1) - 선형 검색, 이진 검색 - 가장 최선의 경우를 생각하면 보자마자 내가 찾고자 하는 값이 나오는 경우이므로
