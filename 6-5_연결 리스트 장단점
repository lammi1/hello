배열과 비교해서 연결 리스트는 새로운 값을 추가할 때 다시 메모리를 할당하지 않아도 된다는 장점이 있다.
배열처름 숫자를 추가하기 위해서 크기를 조절하고 기존의 값을 모두 옮기지 않아도 된다.
역동성을 추가한 대신에 단점은

단점은 연결리스트에서는 임의 접근이 불가능하다. (random acess)
리스트의 처음부터 끝까지 가기 위해서는 포인터를 따라가야하고 중간의 값으로 바로 갈수는 없다.
그러면 이진 탐색을 못한다. 

연결 리스트의 실행시간을 보면
search 는 다시 O(n)이 되었다.
binary search 는 O(log n)이었던 것에 비해 불리해진다.

insert의 경우에는 최악의 경우에도 O(n)이 된다.
